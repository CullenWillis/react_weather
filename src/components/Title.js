import React from "react";

const Title = () => (
  <div>
    <h1 className="title-container__title">Weather Finder </h1>
    <h3 className="title-subtitle">
      Find out the tempature, conditions and more...{" "}
    </h3>
  </div>
);

export default Title;
